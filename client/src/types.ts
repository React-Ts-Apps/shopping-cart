export type ItemProps = {
    id: number;
    name: string;
    price: number;
    description: string;
    imgSrc: string;
}
export type UserProps = {
    name: string;
    email: string;
    password: string;
}